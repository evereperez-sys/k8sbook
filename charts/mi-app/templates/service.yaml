apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi-app.fullname" . }}
  labels:
    app: {{ include "mi-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "mi-app.name" . }}